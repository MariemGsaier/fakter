import { HistoriqueLigneDevise } from './historique-ligne-devise.model';

describe('HistoriqueLigneDevise', () => {
  it('should create an instance', () => {
    expect(new HistoriqueLigneDevise()).toBeTruthy();
  });
});
