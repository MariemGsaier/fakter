import { BankaccountDevise } from './bankaccount-devise.model';

describe('BankaccountDevise', () => {
  it('should create an instance', () => {
    expect(new BankaccountDevise()).toBeTruthy();
  });
});
