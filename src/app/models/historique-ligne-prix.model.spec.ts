import { HistoriqueLignePrix } from './historique-ligne-prix.model';

describe('HistoriqueLignePrix', () => {
  it('should create an instance', () => {
    expect(new HistoriqueLignePrix()).toBeTruthy();
  });
});
